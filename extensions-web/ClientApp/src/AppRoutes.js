import { Extensions } from "./components/extensions";

const AppRoutes = [
    {
        index: true,
        path: "/",
        element: <Extensions />,
    },
];

export default AppRoutes;
